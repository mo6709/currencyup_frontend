// process.env.NODE_ENV === "development" ? this : that
const localBackendURL = "http://localhost:3000/api/v1/";

const herokuBackendUrl = "https://dashboard.heroku.com/apps/currencyup-backend/api/v1/";

export const baseURL = localBackendURL